var defaultSettings = (function () {
    var settings = {
        baseCurrency: "EUR",
        visible: ["all"],
        mainCurrencies: ["USD", "EUR", "GBP", "CHF"]
    };

    return {
        settings: settings
    }
}());