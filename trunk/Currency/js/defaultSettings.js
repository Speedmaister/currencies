var defaultSettings = (function () {
    var settings = {
        baseCurrency: "EUR",
        visible: ["all"],
        mainCurrencies: ["USD", "EUR", "GBP"]
    };

    return {
        settings: settings
    }
}());